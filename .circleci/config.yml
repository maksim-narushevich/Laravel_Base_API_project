version: 2
jobs:
   deploy:
       machine:
         enabled: true
       steps:
          - add_ssh_keys:
                     fingerprints:
                       - "0c:33:a8:8c:40:06:fa:ae:13:c8:63:a6:e7:52:a1:b0"
          - run:
             name: Deploy Over SSH to VPS server
             command: |
               ssh -t $SSH_USER_VPS@$SSH_HOST_VPS "cd /var/www/html/vps/docker_apps/laravel_base_api && touch test.txt && ls -la"
workflows:
  version: 2
  build-and-deploy:
#    jobs:
#      - build:
#                filters:
#                  branches:
#                    only: develop
      - deploy:

          #requires:
          #  - build
          filters:
            branches:
              only: feature/Setting_up_CI_CD